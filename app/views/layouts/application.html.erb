<!DOCTYPE html>
<html>
  <head>
    <title>Bank</title>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <!-- prettier-ignore -->
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <!-- prettier-ignore -->
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application" %>
  </head>

  <body>
    <div class="h-screen flex">
      <div class="w-64 border-2 border-rose-600 h-screen bg-red-300">
        <!-- prettier-ignore -->
        <% if current_user %>
        <%= current_user.username %>
        <%= button_to 'Destroy', '/logout', method: :delete %>
        <a class="block p-4 cursor-pointer" href="/accounts">Accounts</a>
        <a class="block p-4 cursor-pointer" href="/transfers">Transfer</a>

        <% else %>
        <a class="block p-4 cursor-pointer" href="/">Home</a>
        <a class="block p-4 cursor-pointer" href="/login">Login</a>

        <% end %>
      </div>
      <div><%= yield %></div>
    </div>
  </body>
</html>
