<!DOCTYPE html>
<html>
  <head>
    <title>Bank</title>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <!-- prettier-ignore -->
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <!-- prettier-ignore -->
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application" %>
  </head>
  <%= flash.notice %>
  <body>
    <div class="h-screen flex">
      <div class="w-64 border-2 border-rose-600 h-screen bg-red-300">
        <!-- prettier-ignore -->
        <% if current_user %>
        <div class="flex">
          <!-- prettier-ignore -->
          <div class="m-2 p-2"><%= current_user.username %>
          </div>

          <%= button_to 'Logout', '/logout', method: :delete, class: 'bg-red-500
          hover-green-500 rounded-md p-2 text-white m-2' %>
        </div>
        <a class="block p-4 cursor-pointer" href="/accounts">Accounts</a>
        <a class="block p-4 cursor-pointer" href="/transfers">Transfer</a>

        <% else %>
        <a class="block p-4 cursor-pointer" href="/">Home</a>
        <a class="block p-4 cursor-pointer" href="/login">Login</a>

        <% end %>
      </div>
      <div><%= yield %></div>
    </div>
  </body>
</html>
